# MoonFrame 项目申报书

## 基本信息

- 项目名称：MoonFrame——面向字节流的增量消息分帧组件
- 参赛者：Yingqingxue（GitHub ID）
- 联系方式：在报名问卷中填写本人手机号或邮箱
- GitHub：https://github.com/Yingqingxue/moonframe
- 项目方向：新生态项目建设（网络与协议基础组件）
- 是否为移植项目：否，原创项目

## 项目简介

TCP、文件和其他连续字节流不会替应用保留消息边界：一次读取可能只拿到半条消息，也可能把三条消息连在一起。实际写协议时，最容易出错的并不是“在前面加一个长度”，而是半个长度前缀如何暂存、异常长度何时拒绝、流结束时残留数据如何报告，以及攻击者不断发送不完整数据时内存是否失控。MoonFrame 把这些重复工作收进一个小型、可测试的 MoonBit 组件，采用 uLEB128 长度前缀，既提供一次性编码解码，也提供可多次 `feed` 的增量解码器。

MoonBit 生态中已有整数编码库和 HTTP 专用协议实现：前者解决数字如何编码，后者解决某一类协议；MoonFrame 处理的是两者之间更通用的“任意字节流怎样稳定还原消息”问题。解码器同时限制单帧长度和内部缓冲区大小，区分数据不足、畸形长度、帧过大、缓冲超限、校验缺失和校验失败。当前版本已有 13 次有效提交，11 个测试覆盖拆包、粘包、连续多帧、截断、溢出和 CRC-32 校验，并在 wasm、wasm-gc、JavaScript 三个后端运行。

## 预期使用场景

1. **RPC 长连接。** 客户端连续发送多条请求，网络层可能把一个帧拆成多次读取，也可能合并多个帧。调用方把每次读到的字节交给同一个解码器，即可逐条取得完整载荷；半帧保留到下一次读取，不需要业务代码自行拼接。
2. **数据库或自定义服务协议。** 服务端在载荷尚未到齐时就读取到一个异常大的长度。MoonFrame 会先检查最大帧限制并拒绝，而不是按声明长度分配内存；对持续输入但始终不构成完整帧的数据，缓冲区上限提供第二道约束。
3. **日志与文件记录。** 多条二进制记录顺序写入文件，读取时按帧恢复。文件在最后一条记录中间被截断时，调用 `finish` 会返回未完成帧错误，而不是把尾部静默丢弃；需要检查传输或落盘误码时，可启用带 IEEE CRC-32 的帧格式。

## 核心功能范围

- 31 位无符号 uLEB128 长度前缀编码与解析；限制最多五字节，并检查第五字节中的非法高位。
- 单帧和多帧的一次性编码解码；增量 `Decoder` 支持任意位置拆分输入、一次产出多帧、`finish` 与 `reset`。
- 独立的最大帧和最大缓冲限制，在载荷到齐之前也能拒绝越界长度。
- 明确的错误类型：数据不足、畸形变长整数、帧过大、缓冲超限、校验缺失和校验不匹配。
- 可选 IEEE CRC-32 完整性检查，以及可运行的命令行演示。

## 预期验收产物

- 一个公开、使用 Apache-2.0 许可证的 MoonBit 仓库，主分支不少于 10 次有实际内容的提交。
- 可复用的 MoonFrame 库、协议格式说明、公开 API 文档和至少一个命令行示例。
- 覆盖正常帧、拆包、粘包、多帧、截断、变长整数溢出、资源上限和 CRC 错误的核心测试；CI 执行 `moon check`、`moon build` 和三后端测试。
- README 给出安装、最小调用、增量读取方式、错误处理和复现命令；完成审核后发布至 mooncakes.io。

## 工程边界与实现路径

第一阶段不实现 socket、TLS、压缩、身份认证或具体业务协议，也不声称 CRC-32 能防止恶意篡改；它只用于发现偶发传输或存储错误。当前解码器会复制缓冲数据，尚未引入零拷贝切片、环形缓冲或异步 `Reader/Writer` 适配，这些要在语义稳定并有基准数据后再做。常见分帧方案还有分隔符和固定宽度长度字段：分隔符需要转义，固定宽度头部简单但对短消息开销固定；本项目选择有上界的 uLEB128，使短帧头部较小，同时通过五字节限制避免无限解析。

核心层只处理字节，不绑定 I/O，因此同一套行为可用于网络、文件或测试数据。流结束必须显式调用 `finish`，因为“等待下一批数据”和“输入已经截断”在 `feed` 阶段无法区分。`reset` 只允许在调用方确认的新连接或记录边界使用；组件不猜测损坏数据后的重新同步位置，避免把载荷中的普通字节误认成新帧。

## 移植或参考说明

MoonFrame 为原创实现，不是从其他语言仓库移植，也未复制现有项目代码。uLEB128 和 IEEE CRC-32 是公开格式与通用算法，项目会在协议文档中注明其定义。现有 MoonBit 整数编码库可作为更底层的相邻能力，HTTP 库则是上层专用协议；本项目的差异在于提供后端无关、带状态与资源预算的通用分帧过程。
